
<% provide(:title, 'All the users!') %>

<h1>Current users</h1>

<ul class="plainList">

    <% @users.each do |user| %>

    <li>
    	<%= user.id%> : <%= user.name%> : <%= user.email%>
    </li>

<% end %>

</ul>
